# TODO

- [ ] Усилить и покрыть тестами защиту от двойного запуска `tunnelctl`: второй экземпляр с тем же управляющим каналом и конфигурацией должен завершаться без создания второго SOCKS-туннеля. Защита должна работать не только для Планировщика заданий (`IgnoreNew`), но и при ручном параллельном запуске.
- [ ] Добавить диагностическое логирование причин ошибок SSH и внешних утилит: сохранять stderr, exit code, профиль, адрес, команду с замаскированными секретами и контекст попытки, чтобы определять причины неудачных подключений. Добавить ротацию логов: архивировать при размере основного лога более 50 МБ и удалять архивы старше 3 месяцев, сохраняя очередь максимум из трёх архивов.
- [ ] Переработать импорт найденных `ssh -D` туннелей в мастере `bootstrap`.
  - Проблема: мастер не показывает сводку найденных адресов и предлагает подтверждать каждый профиль отдельно; после пустого поиска или отказа не объясняет ручную настройку; после импорта не выводит итоговый список настроенных профилей. При пустом конфиге он также предлагает создать ярлыки, хотя создавать их не для чего.
  - Решение: сначала сообщать общее число уникальных найденных туннелей и выводить нумерованный список с пользователем, адресом `host:port`, локальным SOCKS-адресом, ключом, предлагаемыми именем и алиасом, не раскрывая секреты.
  - Решение: предлагать к импорту не более первых 10 кандидатов. Если найдено больше 10, явно сообщать об ограничении, общем количестве и количестве показанных кандидатов; порядок кандидатов сделать стабильным и понятным, предпочтительно от самых новых записей истории к старым.
  - Решение: запрашивать выбор одной строкой с номерами через запятую, например `1,3,5`; пустой ввод или явный отказ означает отсутствие импорта. Валидировать диапазон, формат и повторы, сообщать о неверных значениях и повторять запрос без частичного изменения конфига.
  - Решение: после подтверждения атомарно добавить только выбранные профили, сохранить конфиг один раз и вывести, что именно настроено: номер, имя/алиас, `user@host:port`, локальный SOCKS-адрес и путь к конфигу. Не считать созданный пустой конфиг настроенным туннелем.
  - Решение: если кандидаты не найдены, пользователь отказался от сканирования или не выбрал ни одного номера, вывести краткую инструкцию по ручной настройке с путём к `tunnelctl.json`, минимальным примером профиля и следующими командами проверки/запуска (`tunnelctl doctor`, `tunnelctl connect <имя>`). Не предлагать ярлыки, пока нет профилей или групп.
  - Тесты: покрыть отсутствие кандидатов, отказ от сканирования, пустой выбор, выбор нескольких номеров, пробелы и дубликаты, неверный формат и диапазон, ровно 10 и более 10 кандидатов, стабильный порядок, единственное сохранение выбранных профилей, итоговую сводку и отсутствие предложения ярлыков для пустого конфига.
